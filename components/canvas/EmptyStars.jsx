'use client'

// This is a placeholder component that renders nothing but the container
// It will be used for server-side rendering
const EmptyStars = () => {
  return <div className="w-full h-auto absolute inset-0 z-[-1]" />
}

export default EmptyStars
